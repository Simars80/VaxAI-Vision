apiVersion: v1
kind: Service
metadata:
  name: {{ include "vaxai-backend.fullname" . }}
  labels:
    {{- include "vaxai-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "vaxai-backend.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
